.sider
  .inner-menu
    ul
      li
        a(href=`/${prefixAdmin}/dashboard`) Tổng quan

      if(role.permissions.includes("employee-category_view"))
        li
          a(href=`/${prefixAdmin}/employee`) Danh sách nhân viên

      if(role.permissions.includes("project-category_view"))    
        li 
          a(href=`/${prefixAdmin}/project`) Danh sách đề án

      if(role.permissions.includes("location-category_view"))  
        li 
          a(href=`/${prefixAdmin}/location`) Danh sách địa điểm phòng

      if(role.permissions.includes("department-category_view"))  
        li 
          a(href=`/${prefixAdmin}/department`) Danh sách khoa

      if(role.permissions.includes("roles_view"))  
        li 
          a(href=`/${prefixAdmin}/roles`) Nhóm quyền

      if(role.permissions.includes("roles_permissions"))
        li
          a(href=`/${prefixAdmin}/roles/permissions`) Phân quyền

      if(role.permissions.includes("accounts_view"))
        li
          a(href=`/${prefixAdmin}/accounts`) Tài khoản
